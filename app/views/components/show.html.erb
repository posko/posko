<section class="content-header">
</section>
<section class="content">
  <div class="box">

    <div class="box-body">
      <h2> Component </h2>
      <div> Title: <%= @component.variant.title %></div>
      <div> Quantity: <%= @component.quantity %></div>
      <div> Cost: <%= @component.cost %></div>

      <%= link_to "Back to Components", components_path(@variant) %>
    </div>
  </div>
</section>

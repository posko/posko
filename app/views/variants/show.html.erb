<section class="content-header">
</section>
<section class="content">
  <div class="box">

    <div class="box-body">
      <h2> Variant </h2>
      <div> Title: <%= @variant.product.title %></div>
      <div> SKU: <%= @variant.sku %></div>
      <div> Variant Type: <%= @variant.variant_type.humanize %> </div>
      <%= link_to "Back to Product", @variant.product %>
    </div>
  </div>
</section>
